services:
  php:
    build:
      context: .
      target: runner
      args:
        PHP_VERSION: 8.5.1
    user: "${UID}:${GID}"
    entrypoint: ["php"]
    environment:
      - XDEBUG_MODE
    volumes:
      - .:/app

  composer:
    extends: php
    entrypoint: ["composer"]
